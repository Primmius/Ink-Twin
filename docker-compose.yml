services:
  inktwin:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        GEMINI_API_KEY: ${GEMINI_API_KEY}
    image: inktwin:latest
    container_name: inktwin
    restart: unless-stopped
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
